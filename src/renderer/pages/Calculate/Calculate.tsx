import Navigation from '../../components/Navigation';

const Calculate = () => {
    return (
        <div className="p-8">
            <Navigation title="Calculate" />
        </div>
    );
};

export default Calculate;